server:
  port: 8089
spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
  datasource:
    url: jdbc:mysql://localhost:3306/siga?serverTimezone=America/Santiago&useSSL=false
    username: servoclay@gmail.com
    password: jIq2atp81QIh

logging:
  file: logs/sga-api.log
  level:
    org:
      hibernate:
        SQL: DEBUG
